doctype html
html
    head
        meta(charset="utf-8")
        meta(name="viewport" content="width=device-width, initial-scale=1")
        title Library App -- #{title}
        link(rel="stylesheet" href='/bower_components/bootstrap/dist/css/bootstrap.css')
        style.
            #home {
            font-family: "Comic Sans MS", cursive, sans-serif;
            font-size: 70px;
            letter-spacing: -2px;
            word-spacing: 3px;
            color: #1C80FF;
            font-weight: normal;
            text-decoration: none solid rgb(68, 68, 68);
            font-style: italic;
            font-variant: small-caps;
            text-transform: capitalize;
            }
        script(src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js")
        script.
            function books() {
            console.log('books.html');
            $.ajax({
            type: 'POST',
            url: 'http://localhost:3000/books'
            });
            window.location = 'http://localhost:3000/books.html';
            }
    body
        nav.navbar.navbar-expand-lg.navbar-light.bg-light(style="padding:1%;")
            .container-fluid
                button.navbar-toggler(type="button" data-bs-toggle="collapse" data-bs-target="#navbarTogglerDemo03" aria-controls="navbarTogglerDemo03" aria-expanded="false" aria-label="Toggle navigation")
                    span.navbar-toggler-icon
                a.navbar-brand(style="color: #1C80FF;") Library
                #navbarTogglerDemo03.collapse.navbar-collapse
                    ul.navbar-nav.me-auto.mb-2.mb-lg-0
                        li.nav-item
                            a.nav-link.active(aria-current="page" href="/") Home
                        li.nav-item
                            a.nav-link(href="/books") Books
                            //- a.nav-link(type="button" onclick="books()") Books
                        li.nav-item
                            a.nav-link.disabled(href="#" tabindex="-1" aria-disabled="true") Disabled
        block content
        br
        hr
    footer
        p Copyright &copy; 2020.